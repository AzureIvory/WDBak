# WDBak
> ### Developing

# runtime
>[webview2](https://developer.microsoft.com/zh-cn/microsoft-edge/webview2)

# build
>go build

# TODO
> 支持通配符/正则
> 支持跳过某些后缀文件
> 支持搜索文件（通配符/正则）
> 支持快捷方式跳转到原文件
> 默认不上传快捷方式
> 并发数量默认取cpu核心数/线程数（数量为0时取cpu核心数/线程数）
> cfg支持导出/导入配置文件
> 完善UI和各类协议解释
> 当使用SMB协议时默认将用户名改为Guest
> 写入成功时字样应该显眼或者弹出信息框